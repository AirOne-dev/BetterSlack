---
name: all
group: settings
title: api.settings
signature: (): Record<string, unknown>
---

Everything this mod has stored, as one object.

```js
const values = api.settings.all();   // everything this mod has stored
```
